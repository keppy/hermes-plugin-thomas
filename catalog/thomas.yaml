# Submitted to NousResearch/hermes-agent as plugin-catalog/thomas.yaml.
# Kept here so the reviewed pin sits next to the code it pins; the copy in
# plugin-catalog/ is the one that installs.
name: thomas
repo: https://github.com/keppy/hermes-plugin-thomas
sha: 369aa76700cdea797c7e94d6e97b882a2ae6f390
description: Fine-tune a small calibrated encoder classifier on Modal and rate it with gonogo.
  Every GPU launch goes through the human approval gate with the exact config shown; data
  checks and evaluation run locally for free.
maintainer: keppy
tier: community
category: tools
requires_hermes: ">=0.21"
version: "0.1.0"
docs_url: https://github.com/keppy/hermes-plugin-thomas#readme
capabilities:
  provides_tools:
    - thomas_check_data
    - thomas_encoder_train
    - thomas_run_status
    - thomas_encoder_eval
  provides_hooks:
    - pre_tool_call
  provides_middleware: []
  requires_env: []
